import { AmazonShopValidators } from './amazon-shop-validators';

describe('AmazonShopValidators', () => {
  it('should create an instance', () => {
    expect(new AmazonShopValidators()).toBeTruthy();
  });
});
